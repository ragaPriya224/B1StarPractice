package practice.abstraction.interfaceDemo;

public interface Mobile {

	 void call();  //by default, all the methods will be considered as abstract
	// by default, it will have public access specifier 
	 //100% abstraction -> interface concept 

}
