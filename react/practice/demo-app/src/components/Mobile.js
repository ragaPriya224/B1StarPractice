import React from "react"; //import react library

function Mobile() {   //functional component
    const name ="i'm samsung";
  return (
   <div>{name}</div>    //jsx -> combo of js + html
   
  );
}

export default Mobile; // exporting it, so that it can be imported elsewhere